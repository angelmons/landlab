name: Draft PDF

# Only trigger when files in the river_temperature_dynamics folder or this workflow change
on:
  push:
    paths:
      - 'joss/in_preparation/river_temperature_dynamics/**'
      - '.github/workflows/draft-pdf.yml'

jobs:
  paper:
    runs-on: ubuntu-latest
    name: Paper Draft
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Build draft PDF
        uses: openjournals/openjournals-draft-action@master
        with:
          journal: joss
          # Path to the paper within your repo
          paper-path: joss/in_preparation/river_temperature_dynamics/paper.md
          
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: paper
          # This must match the directory of the input paper.md
          path: joss/in_preparation/river_temperature_dynamics/paper.pdf

      - name: Commit PDF to repository
        uses: EndBug/add-and-commit@v9
        with:
          message: '(auto) Paper PDF Draft for RiverTemperatureDynamics'
          # Path to commit
          add: 'joss/in_preparation/river_temperature_dynamics/paper.pdf'
